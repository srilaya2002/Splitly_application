name: spltly
description: Split expenses with anyone, anywhere.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.3.0
  go_router: ^13.2.0
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  image_picker: ^1.1.1
  file_picker: ^8.0.3
  uuid: ^4.4.0
  intl: ^0.19.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
